# Setup

## Requirements

- Node.js 18 or newer
- npm

## Start locally

```bash
npm install
npm run dev
```

Open the URL shown in the terminal. By default it is:

```text
http://127.0.0.1:5173/
```

## Build

```bash
npm run build
```

The production files will be created in `dist`.
